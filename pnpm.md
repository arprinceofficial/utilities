## (Option 1) Install via Corepack (recommended for Node 16.13+ or 18+)
- npm install -g pnpm
- pnpm -v

## (Option 2) Install via Corepack (recommended for Node 16.13+ or 18+)
- corepack enable
- corepack prepare pnpm@latest --activate
- pnpm -v
