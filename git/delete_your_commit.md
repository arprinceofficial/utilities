### REMEMBER IT WILL DELETE PERMANENTLY

## First checkout that specific commit and use it
```bash
git reset --hard HEAD~1
```
## Force Fully update the branch `
```bash
git push origin HEAD --force
```